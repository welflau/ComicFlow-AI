# 组件目录

此目录用于存放可复用的组件文件。

## 组件开发规范

1. 每个组件应该有清晰的职责
2. 组件应该是可复用的
3. 组件应该有良好的文档说明
4. 组件应该有适当的测试覆盖

## 目录结构建议

```
components/
├── common/          # 通用组件
├── layout/          # 布局组件
├── forms/           # 表单组件
└── ui/              # UI组件
```